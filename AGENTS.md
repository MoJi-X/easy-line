# AGENTS

## 开发流程
1. 需求、范围、验收标准不清时先补齐上下文，不直接写代码。
2. 文档冲突时以 `docs/需求规格说明书.md` 作为需求主基线；涉及当前已拆分增量模块的范围、依赖顺序和延期项时，以 `specs/00-overall-plan.spec.md` 为当前迭代基线，架构、数据库、API 文档只用于补充实现边界。
3. 先维护 `specs/*.spec.md`，再按依赖顺序编码；每次只推进一个可运行切片。
4. 每日都要有可验证结果，优先保证聊天主链路跑通，再按 spec 依赖顺序推进告警分析与工单创建链路、动态任务链路和定时推送链路。
5. 需求变更、取舍结论和延期项必须同步写回 spec，不只保留在对话里。
6. 迭代顺序固定为：启动与 LINE 链路 -> Agent 编排与 Tavily -> 告警 Tool 接入 -> 告警分析工作流 -> 工单创建与结果回写 -> 动态任务 CRUD 与持久化 -> 告警信息定时推送执行 -> 治理与收尾。

## 代码规范
1. 维持 Demo 的精简架构：TypeScript + Express + `@line/bot-sdk` + LangChain + `node-cron` + `axios` + `dotenv`。
2. 优先复用成熟框架能力，不为了 Demo 额外引入数据库、中间件平台或复杂抽象。
3. 函数保持单一职责，模块边界清晰，避免跨层直接依赖和重复封装。
4. 外部调用、关键分支和异常路径必须有错误处理与可读日志。
5. 未经 spec 明确批准，不扩展高成本优化项。
6. 源码、配置、JSON 和文档文件统一使用 `UTF-8` 编码保存；中文文案禁止写成 `\uXXXX` 转义或以本地编码落盘，避免再次出现乱码。

## 接口调用标准
1. `POST /webhook` 按 LINE 回调约定处理，必须接入签名校验中间件。
2. 内部管理接口统一返回 JSON，并明确成功结构、错误码和失败原因。
3. 外部 API 调用必须配置超时；重试和降级只做 Demo 必要级别。
4. 不在响应体和日志中暴露密钥、令牌或完整敏感配置。

## 数据存储与数据库操作规范
1. Demo 阶段不引入正式数据库，默认使用按 `userId` 分组的内存会话容器、内存任务索引和 `src/config/tasks.json`。
2. 对话上下文与告警/工单工作流状态按 `userId` 分组管理；消息记忆限制保留最近 3 轮对话，工作流状态只保留 Demo 必需字段。
3. 动态任务允许在运行时进行 CRUD，但持久化仍以 `src/config/tasks.json` 为准，不引入数据库后台。
4. 如果未来要引入 PostgreSQL 或 Redis，必须先补 spec，再实施迁移。

## Git 与终端约束
1. 默认使用 Git Bash 执行 Git 相关命令，不使用 PowerShell 作为 Git 操作终端，除非确有无法替代的 Windows 兼容性需求。
2. 每次完成一轮可验证改动后，必须执行 `git status` 自检，并创建一次 `git commit`，不把多轮无关改动长期堆积在工作区。
3. 提交信息使用清晰的 Conventional Commits 风格，能体现本次改动的模块和目的。
4. 提交前必须确认不包含密钥、令牌、`.env` 敏感内容或无关脏文件。

## 团队协作机制
1. 一个 `*.spec.md` 对应一个可交付模块，不把多个模块混到同一次实现里。
2. 每次提交都要说明当前完成了哪个 spec、阻塞点是什么、下一步接哪一个 spec。
3. Demo 阶段允许暂缓性能优化和完整安全治理，但必须显式记录延期项。
4. 当 `AGENTS.md` 与新 spec 产生冲突时，应先同步修订 `AGENTS.md`，再继续开发。
